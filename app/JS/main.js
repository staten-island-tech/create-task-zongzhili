import "/CSS/style.css";

let board = Array(6)
  .fill()
  .map(() => Array(7).fill(null));
console.log(board);

let currentPlayer = "player1";

function createBoard() {
  for (i = 0; i < 6; i++) {
    for (i = 0; i < 7; i++) {}
  }
}
